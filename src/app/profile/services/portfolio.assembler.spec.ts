import { PortfolioAssembler } from './portfolio.assembler';

describe('PortfolioAssembler', () => {
  it('should create an instance', () => {
    expect(new PortfolioAssembler()).toBeTruthy();
  });
});
